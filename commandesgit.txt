# Premier éxécutions

git config --global user.name "Ton nom"
git config --global user.email "l'email que tu utilise pour github"
prend un dossier pour le projet vide
git init
git branch -M main
git remote add origin https://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@github.com/Eliossore/sae201.git
git pull origin main

# A chaque fois que quelqu'un change quelque chose

git pull origin main

# A chaque fois que tu modifie quelque chose

git add .
git commit -m "Les modifs faites"
git push origin main

